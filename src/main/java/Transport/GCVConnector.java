package Transport;

import AgenteUDP.StreamOUT;

public class GCVConnector implements Connector {



    public Channel connect(String ip) throws InterruptedException {
        new StreamOUT(int capacity, int packetSize, InetAddress ip, int port)
    }
}
